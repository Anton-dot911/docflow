.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
dist/
.env

# T11 golden dataset (Goldsmith-owned data — LESSONS §9; re-fetch via `make eval`).
# The eval *script* and eval_runs/*.json results are committed; the raw fixtures
# and downloaded files are not (see docs/decisions.md).
data/golden/
